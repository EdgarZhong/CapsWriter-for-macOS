# CapsWriter-Offline 协作规则

## 用户规则

- 永远用中文与用户沟通。
- 所有新增或修改代码必须附带详细中文注释。
- 当用户要求查看项目、总览项目、扫描目录时，必须递归查看目录结构后再总结。
- 当需求存在歧义时，不得自行脑补后直接开工，必须先收敛口径、给出建议、等待用户确认。
- 在执行代码修改、命令运行、文件移动前，必须先说明意图，再执行工具调用。

## 核心文档职责

- `readme.md`：唯一的用户向 README，记录项目介绍、安装、使用、故障排查及用户可见功能方向；不混入开发回归细节，主分支发布只保留这份文件。
- `README.dev.md`：唯一的开发向 README，只记录稳定项目事实、架构、目录、环境、开发命令与重要文档索引；不承载动态看板，开发分支维护。
- `AGENTS.md`：只记录通用规则、开发流程、协作边界、验收标准、文件安全策略。
- `CLAUDE.md`：只记录当前阶段目标、进度同步、任务看板、阶段决策、风险与执行状态。

## macOS 平台与工具链基线

- CapsWriter for macOS 只支持 Apple Silicon（`arm64`）与 macOS 26.0 及以上；不再维护 macOS 25 及以下系统或 Intel 架构兼容路径。
- Dashboard 固定使用 Command Line Tools for Xcode 26.6、Swift/SwiftPM 6.3.x 和 macOS 26.5 SDK 构建；不得以 CLT 27、Swift 6.4 或其他 SDK 作为默认基线。
- 最低系统已收敛到 macOS 26.0，平台代码可以直接使用新版 macOS API，不需要为旧系统添加回退；若保留旧回退，必须有明确迁移理由。
- Dashboard 只支持普通窗口，系统全屏与 full-screen tiling 均禁用；不得重新引入全屏专用布局或通过视觉参数修补全屏模式。

## 文档维护规则

- `main` 为发布分支，`mac-dev` 为开发分支；两份 README 在同一工作区按受众分工；开发分支的 `readme.md` 与 main 当前发布版本保持一致，未发布能力不提前写入用户说明，合并时不得以开发说明覆盖用户入口。发布版本历史维护在 main 根目录 `CHANGELOG.md`。

- 缺乏信息时，优先通过 `README.dev.md` 的文档索引寻找已有资料。
- `docs/` 存放专项规格、专项 SOP、调试说明、测试说明；除非用户明确要求，不主动查看 `docs/archived/`。
- 用户在会话中确认的口径收敛，必须尽快同步到对应文档。
- 新会话首轮必须查看项目内 `AGENTS.md` 与 `CLAUDE.md`。
- 新会话首轮必须用 `git` 查看近两次 Markdown 文档变更，避免偏离最新口径。

## 文件安全规则

- 永远不要删除文件。
- 需要废弃的文件一律移动到项目根目录 `.archive/`。
- `.archive/` 必须保持 Git 忽略状态。
- 覆盖重要文件前，先在 `.archive/` 中备份。
- 禁止执行 `rm`、`git clean`、`git reset --hard`、`sudo`、强推历史等破坏性操作。

## 桌面安全规则

- **禁止在用户桌面上创建全屏或大面积覆盖窗口**（纯色背板、采样底图、遮罩层等，如 `/tmp/solid_backdrop` 这类工具）用于测试、采样或验收；2026-09-20 因此导致用户被迫重启电脑，属严重事故。
- 需要纯色背景做对比验证（如双色背景差值测试）时，改用不遮挡桌面的方式：窗口截图 alpha 通道分析、利用现有应用窗口做背景、或在用户明确在场并同意后短时进行且用完立即清理。
- 任何会在用户屏幕上产生可见窗口、覆盖层或抢占前台的验证手段，动手前必须先向用户说明并取得同意。

## 代码规范

- 优先保持现有架构稳定，在此基础上做最小必要改动。
- 涉及平台差异时，优先新增抽象层或平台实现，不要把条件分支散落到业务流程各处。
- 所有平台相关能力至少覆盖以下维度：快捷键监听、文本注入、剪贴板/选中文本、前台窗口识别、推理后端选择。
- 修改配置项、运行方式、依赖方式时，必须同步更新文档入口。
- 架构迁移必须区分“已确认的目标架构”和“当前运行实现”；历史架构约束以最新明确决策为准，文档定稿不等于代码或部署已完成。
- 服务化抽取与算法优化分别界定范围；未明确纳入本轮的行为改动不得夹带实施，已知缺陷应保留记录并说明兼容验证边界。
- 新增注释必须解释设计意图、平台差异或非直观逻辑，避免只复述代码表面行为。
- 默认使用 ASCII；仅在中文文档、中文注释和已有中文文件中使用中文。

## 架构归属与服务化判断准则

2026-09-23 产品化架构定稿的通用判断原则（细则与拓扑见 `docs/macos-产品化架构定稿.md`，为架构最高口径）。施工中遇到"该放 Swift 还是 Python""要不要独立成服务"的问题，必须按本节判断，不得凭惯性决定。

### Swift 还是 Python

- 属于以下者**优先 Swift 原生**：macOS 系统交互、用户实时输入体验、UI、App 状态、快捷键、权限、焦点、剪贴板、音频设备、session 生命周期、用户实时数据采集。
- 属于以下者**可以 Python**：算法、模型推理、离线数据处理、benchmark、研究工具。
- 用户正常输入链中的数据采集（录音 session、raw/corrected/final 文本、编辑框确认、标注触发）归 Swift；脱离实时链的离线数据工程（清洗、dataset build、评测、报告）归 Python。不因"最终用于研究"而把实时链组件留在 Python。

### 要不要独立成服务

- **只有**确实需要独立生命周期、独立模型资源、长期常驻预加载、独立崩溃隔离、独立流式通信、可单独启停替换时，才用「独立 process + WebSocket + 端口」。
- 不因"它是 Python"就自动服务化；不因"想统一架构"就把多个无关服务揉成一个总 Server / 统一 Engine / 统一 RPC 层。
- **模块独立 ≠ 进程独立**：客户端内部算法组件（当前为热词）不服务化，以 App 托管 private helper 存在（轻量本地 IPC，不监听端口、不做独立 health check、随 App 启停）；算法实现语言不构成服务化理由。

### 产品行为红线

- 文本上屏**唯一路线 Paste**（NSPasteboard + CGEvent Cmd+V）；任何场景（含未来流式分段上屏）不得新增第二注入后端或多级 fallback 链。
- 焦点策略（录音开始位置 FocusAnchor，失效禁止本次自动上屏并发系统通知，不 fallback 当前焦点）与剪贴板保留策略（基于 changeCount 的安全恢复）是两个正交设置，实现时不得耦合、不得为焦点失败偷偷改变剪贴板行为。
- 结果数据必须保留 raw_text / corrected_text / final_text / hotword_matches 阶段，热词层不得洗掉 ASR 原始输出。

## 开发测试 SOP

1. 会话启动

- 查看 `AGENTS.md` 与 `CLAUDE.md`。
- 递归扫描项目目录，确认本次涉及的代码区域。
- 查看近两次 Markdown 提交，理解最新文档变化。

2. 方案收敛

- 对用户目标做简明复述。
- 在动手前说明本轮准备执行的命令或修改范围。
- 若存在高风险分支或多种技术路径，先给出推荐路径与理由。

3. 实施变更

- 优先使用仓库内现有结构和已有工具，不轻易引入新框架。
- 重要文件先备份到 `.archive/`。
- 大规模修改时按步骤推进，不一次性并发改很多区域。
- 运行 Git 长输出命令时统一使用 `git --no-pager`。

4. 验证闭环

- 先跑最小必要的定向验证，再决定是否扩大验证范围。
- 无法验证的内容必须明确说明原因、阻塞点和残余风险。
- 验证结果要回写 `CLAUDE.md` 的任务看板或状态区。

5. 收尾同步

- 用户安装与使用事实写回 `readme.md`；稳定开发事实与专项文档索引写回 `README.dev.md`。
- 规则、流程、验收要求写回 `AGENTS.md`。
- 阶段任务、状态、决策写回 `CLAUDE.md`。

## 通用验收要求

- 代码、配置、文档三者口径一致。
- 平台改造必须明确区分“已验证可用”和“仅完成代码接入”。
- 每次提交给用户的结果都要说明本轮完成项、未完成项、验证情况。
