# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与[语义化版本](https://semver.org/lang/zh-CN/)。

版本号处于 0.x 阶段：本 fork 尚未正式产品化，minor 版本之间可能包含行为调整。0.1.0 与 0.2.0 为事后补记的历史节点，首个实际发布的版本是 0.3.0。

## [0.3.0] - 2026-09-19

首个正式发布。重点：MLX 后端两项精度修复、服务端高延迟与乱序修复、麦克风稳定性与开麦延迟优化、模型权重常驻开关。

### 修复

- **中英混说误转全英文**：自动语言模式下中英混合口述会被带偏成几乎全英文输出；现默认预置正文前缀修复，显式指定语言时的行为不变
- **长句尾部丢字**：长音频末尾不完整块此前未按官方语义补齐，导致句尾汉字漏识别或识别错误；修复后句末文字更完整
- **服务端高延迟与结果乱序**：修复音频包持续到达时服务端反复等包、单句长时间排队的问题；同一句子的音频严格按顺序处理，后录的句子不再抢到前句之前返回
- **麦克风松手后不关闭**：修复在麦克风尚未完全打开时松手、停止请求被丢弃导致麦克风持续占用的问题
- **编辑框圆角瑕疵**：修复编辑框毛玻璃背景在底部两角露出白色矩形的问题

### 新增

- **模型权重常驻开关**：`config_server.py` 的 `enable_wired_memory`（默认开）将约 2.5 GB 模型权重物理锁定在内存中，明显减少长时间不用后首次识别的调页延迟；内存紧张时可关闭，允许系统正常换出。锁页失败会明确报错，不再静默降级
- **录音设备选择**：`config_client.py` 新增 `macos_mic_device`，默认 `default` 跟随系统当前默认输入设备（插拔耳机自动跟随）；常戴耳机的用户可改为 `'builtin'` 固定使用 Mac 内建麦克风

### 变更

- **开麦更快**：macOS 采集改用 20ms 音频块与低延迟模式，按下到开始录音的首帧延迟实测降低约 33ms；空闲时不占用麦克风
- **麦克风关闭更可靠**：关闭过程严格校验系统返回的错误码；关闭异常时明确通知并暂停新录音，避免重复占用

## [0.2.0] - 2026-08-25

### 新增

- **菜单栏图标与下拉菜单**：原生矢量图标常驻菜单栏，自动适配深 / 浅色外观；点击可查看状态、复制最近结果、编辑热词、重启或退出
- **原生系统通知**：改用 macOS 通知中心发送，归属 CapsWriter 身份
- **编辑框标注模式**（可选）：识别结果先进入原生编辑框，Enter 确认即时上屏、Esc 放弃；确认与纠正内容可保存为可追溯记录
- **权限引导重写**：辅助功能与输入监控两项权限的状态机引导；权限异常时用 `capswriter reset-permissions` 一键重置后重新授权
- **MLX 专用 Runner**：Qwen MLX 后端改经 `mlx-qwen3-asr` 子仓库的专用 Runner 统一管理推理配置

### 修复

- 修复 `.app` 启动器写死开发者机器路径，导致更换用户名或机器后直接闪退的问题
- 修复开机自启（launchd）环境下找不到 FFmpeg、录音被静默保存为大体积 WAV 的问题
- 修复 Caps 长按启动窗口内松手导致麦克风不关闭的竞态
- 修复撤销系统权限后菜单栏状态不更新、以及若干进程生命周期问题

## [0.1.0] - 2026-05-24

首个 macOS 可用形态（内部曾称"macOS 适配版 v1.0"，现按 0.x 阶段重新编号）。

### 新增

- **Apple MLX 推理后端**：接入 Qwen3-ASR-1.7B 本地推理，完全离线，松手后约 0.3～0.6 秒返回结果
- **Caps Lock 语音输入**：通过 hidutil + CGEventTap 实现长按录音、短按保留大小写切换
- **CapsWriter.app**：以原生 .app 运行，录音时系统麦克风指示显示「CapsWriter」
- **launchd 双服务架构**：client 与 server 独立托管，登录自动启动
- **`capswriter` CLI**：统一控制入口（`start / stop / restart / status / install / uninstall / doctor / remap`）
- **`install.sh`**：一键安装依赖并把 `capswriter` 注册为全局命令
